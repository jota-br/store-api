type Query {
    reviews: [Review]
    getReviewsById(id: ID!): Review
}

type Mutation {
    addNewReview(customer: Int!, product: Int!, rating: Float!, comment: String): Review
}

type Review {
    id: ID!
    customer: Customer!
    product: Product!
    rating: Float!
    comment: String
    active: Boolean
    createdAt: Int!
}