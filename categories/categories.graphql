type Query {
    categories: [Category]
    category(id: ID!): Category
}

type Mutation {
    addNewCategory(name: String!, description: String): Category
}

type Category {
    id: ID!
    name: String!
    description: String
}