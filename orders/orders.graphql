type Query {
    orders: [Order]
    order(id: ID!): Order
}

type Mutation {
    addNewOrder(orderDate: Int!, totalAmount: Float!, customer: Int!): Order
}

type Order {
    id: ID!
    customer: Customer
    orderDate: Int!
    totalAmount: Float!
}